@model LS_BlogLuca.EF.Etichette

@{
    ViewBag.Title = "Edit";
    Layout = "~/Areas/Backoffice/Views/Shared/_Layout.cshtml";
}




<div id="page-wrapper">
    <div class="row">
        <div class="col-lg-12">
            <h1 class="page-header">Etichetta</h1>
        </div>
        <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    <div class="row">
        <div class="col-lg-12">
            <div class="panel panel-default">
                <div class="panel-heading">
                   @if (Model.IdEtichetta == 0)
                   {<text>Inserisci l'etichetta...</text> }
                   else
                   { <text>Modifica l'etichetta...</text> }
                      
                </div>
                <div class="panel-body">
                    <div class="row">
                        <div class="col-lg-12">
                           
                    @using (Html.BeginForm())
                    {
                        @Html.AntiForgeryToken()

                        @Html.HiddenFor(model => model.IdEtichetta)
                              
                                <div class="form-group">
                                    <label>Chiave</label>
                                  
                                    @Html.EditorFor(model => model.Chiave, new { htmlAttributes = new { @class = "form-control" } })
                                    @Html.ValidationMessageFor(model => model.Chiave, "", new { @class = "text-danger" })
                                </div>
                                <div class="form-group">
                                    <label>Valore</label>
                                   
                                    @Html.EditorFor(model => model.Valore, new { htmlAttributes = new { @class = "form-control" } })
                                    @Html.ValidationMessageFor(model => model.Valore, "", new { @class = "text-danger" })

                                </div>
                                <div class="form-group">
                                    <label>Valore EN</label>

                                    @Html.EditorFor(model => model.Valore_en, new { htmlAttributes = new { @class = "form-control" } })
                                    @Html.ValidationMessageFor(model => model.Valore_en, "", new { @class = "text-danger" })

                                </div>


                        if (Model.IdEtichetta == 0)
                        {
                                    <button type="submit" value="Create" class="btn btn-primary"><i class="fa fa-pencil"></i>&nbsp&nbspInserisci</button>
                                }
                                else
                                {
                                    <button type="submit" value="Save" class="btn btn-primary"><i class="fa fa-pencil"></i>&nbsp&nbspSalva</button>}

                                }
                         
                        </div>
